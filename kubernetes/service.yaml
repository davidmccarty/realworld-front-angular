apiVersion: v1
kind: Service
metadata:
  name: realworld-front-angular-service
  namespace: realworld-front-angular
  labels:
    app: realworld-front-angular
spec:
  selector:
    app: realworld-front-angular
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
